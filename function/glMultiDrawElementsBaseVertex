gl:
  name: glMultiDrawElementsBaseVertex
  params:
  - mapping: mode
    name: mode
    type: GLenum
  - mapping: '&counts[0]'
    name: count
    type: GLsizei const*
  - mapping: type
    name: type
    type: GLenum
  - mapping: '&indices[0]'
    name: indices
    type: GLvoid const* const*
  - mapping: elements.size()
    name: drawcount
    type: GLsizei
  - mapping: '&bases[0]'
    name: basevertex
    type: GLint const*
  return: void
gtu:
  name: draw_elements
  params:
  - name: mode
    type: gtu::constant const
  - name: type
    type: gtu::constant const
  - name: elements
    type: std::vector< std::tuple< gtu::size, gtu::buffer_ref, int32_t > > const&
  precall: |
    std::vector< gtu::size >   counts;
    std::vector< void const* > indices;
    std::vector< int32_t >     bases;
    typedef std::tuple< gtu::size, gtu::buffer_ref, int32_t > vtype;
    std::transform(elements.begin(), elements.end(), std::back_inserter(counts),  [](vtype const& e) { return std::get< 0 >(e); });
    std::transform(elements.begin(), elements.end(), std::back_inserter(indices), [](vtype const& e) { return std::get< 1 >(e); });
    std::transform(elements.begin(), elements.end(), std::back_inserter(bases),   [](vtype const& e) { return std::get< 2 >(e); });
  return: void
